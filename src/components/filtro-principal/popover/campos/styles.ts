import styled from 'styled-components';
import { Colors } from '~/styles/colors';

export const LabelPopover = styled.div`
  font-size: 11px;
  font-weight: 400;
  color: ${Colors.Label};
  margin-bottom: 10px;
`;
